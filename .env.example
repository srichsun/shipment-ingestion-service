# Copy to .env and adjust. Never commit the real .env.

# Comma-separated list of valid API keys.
# Multiple keys let different clients rotate independently.
API_KEYS=dev-local-key

# Comma-separated allowlist of carriers.
# Config-driven so new carriers can be added without a code change.
ALLOWED_CARRIERS=UPS,FedEx,DHL
